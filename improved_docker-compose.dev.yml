version: '3.8'
services:
  app:
    build: .
    command: npm run dev -- --host 0.0.0.0
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    ports:
      - "5173:5173"
    environment:
      - NODE_ENV=development
volumes:
  node_modules: