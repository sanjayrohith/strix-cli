version: '3.8'
services:
  app:
    build: .
    restart: unless-stopped
    healthcheck:
      test: wget --no-verbose --tries=1 --spider http://localhost:80/ || exit 1
      interval: 10s
      timeout: 5s
      retries: 3
    ports:
      - "80:80"